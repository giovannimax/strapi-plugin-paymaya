{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "Paymaya.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/success",
      "handler": "Paymaya.verify",
      "config": {
        "policies": []
      }
    }
  ]
}
